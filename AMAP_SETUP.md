# 高德 Android 地图配置

项目已经接入高德 3D 地图 SDK。为了避免把 Key 提交到 GitHub，真实地图只在本机配置 Key 后启用；未配置时会自动显示原来的内置地图，不影响编译和实验流程。

## 1. 申请 Android Key

1. 登录[高德开放平台控制台](https://console.amap.com/dev/index)。
2. 创建一个应用，例如 `SAGE Motion 实验`。
3. 添加 Key，平台选择 `Android`。
4. 填写包名：`cn.tsinghua.sagemotion`。
5. 当前高德控制台会分别要求“调试版安全码 SHA1”和“发布版安全码 SHA1”。现在发出的测试 APK 使用调试签名，可在项目目录运行：

   ```powershell
   & "$env:JAVA_HOME\bin\keytool.exe" -list -v -alias androiddebugkey -keystore "$env:USERPROFILE\.android\debug.keystore" -storepass android -keypass android
   ```

   复制输出中的 `SHA1` 到调试版安全码。当前构建机的调试 SHA-1 是 `FD:78:1C:99:F5:C7:0C:FD:70:B6:2D:7C:3B:78:31:F6:B0:79:63:9C`。
   正式发布前再创建正式签名证书，把它的 SHA-1 填入同一项配置的发布版安全码。

## 2. 在本机填写 Key

打开项目根目录的 `local.properties`，增加：

```properties
AMAP_API_KEY=这里粘贴Android_Key
AMAP_STYLE_ID=
```

`local.properties` 已被 Git 忽略，不会上传 Key。也可以在构建前设置同名环境变量。

重新构建并安装 APK。第一次进入实验主界面时会出现真实地图隐私提示；同意后才创建高德 `MapView`。

## 3. 正式实验前的隐私工作

- 在实验知情同意书或应用隐私说明中明确列出高德地图 SDK、使用目的、可能处理的数据和高德隐私说明链接。
- 当前实现不申请定位权限、不显示定位蓝点，也不会把参与者编号发送给高德。
- 如果清除了应用数据或重装应用，需要重新确认地图隐私提示。

## 4. 地图 UI 个性化

### 高德控制台样式（推荐）

1. 登录[高德开放平台控制台](https://console.amap.com/)，进入[自定义地图平台](https://lbs.amap.com/product/mapstyle)。
2. 选择“创建地图样式”，从免费版提供的标准模板复制一份。图片创建和智能配色属于付费能力；本项目并不依赖它们，可以按下面给出的色值手动调整。
3. 在左侧分别调整区域面、建筑、水系、天空、道路、标注和行政边界。建议先隐藏与公园实验无关的商业 POI，再调整颜色，避免参与者被无关信息吸引。
4. 点击右上角“保存”→“发布”→“使用方法”，选择 Android。

编辑器顶部出现“以下功能仅在地图 JS API 中生效，Android/iOS 地图 SDK 暂不支持”并不表示整份模板不能用于 Android。它只是在提示：该条横幅点名的编辑器能力不会随样式包落到移动端，例如编辑器内的第三方图层和标注字号调节。道路、绿地、水体、建筑、区域面、标注颜色等基础底图风格仍可正常用于 Android。

如果实验需要额外的路线、打卡点、照片节点或公园边界，应继续由 App 通过 `Polyline`、`Marker`、`Polygon` 等原生覆盖物绘制；这些属于运行时业务图层，不依赖样式编辑器里的“第三方图层”。

### 免费版（本项目推荐）

免费版可创建 1 份样式并离线调用，不需要购买 399 元/月的专业版。下载与当前 Android 地图 SDK 匹配的离线包，解压后把 `style.data`、可选的 `style_extra.data` 和 `textures.zip` 放进：

```text
app/src/main/assets/amap_style/
```

重新构建即可。代码优先使用离线样式；更换样式文件后需要重新发 APK。

### 付费在线 Style ID（可选）

只有希望不重新发 APK 就在线更新风格，或需要图片创建、智能配色、更多模板/样式数量及高级纹理配置时，才建议购买。开通“地图 SDK 在线调用”后，把页面显示的样式 ID 填入 `local.properties`，不要添加 `amap://styles/` 前缀：

   ```properties
   AMAP_STYLE_ID=高德页面显示的样式ID
   ```

重新构建、安装并重新进入地图页面。没有离线 `style.data` 时，代码才会读取该 ID 并调用 `CustomMapStyleOptions.setStyleId(...)`。

不要混用其他 SDK 版本导出的样式文件。免费离线调用在 Android/iOS 上也有少量限制，例如部分要素透明度和按 POI 分类的独立设置并不生效，因此先完成道路、绿地、水体、建筑和文字主色即可。

建议延续当前 SAGE 视觉语言：背景 `#F7F7F2`，公园绿地 `#DDEBCF`，水体 `#CFE7E0`，普通道路 `#E9E6DB`，道路描边 `#CACFC4`，文字 `#52635B`。深绿色主路线与赭色改道提示由 App 自己绘制，不必写进底图样式。

### App 内叠加层

代码中的 `AmapParkMap.kt` 已经：

- 关闭缩放按钮、指南针和比例尺，开启旋转、倾斜与缩放手势；
- 保留高德版权标识；
- 通过高德新版 `RouteSearchV2.calculateWalkRouteAsyn` 请求真实步行路径及备选方案；
- 使用地图原生 `Polyline` 绘制路线，拖动和缩放时路线会与道路同步；
- 读取真实距离、预计步行时间和逐段指令；
- 在参与者主动点击“开始指引”并授权后，以当前位置重新算路、显示位置点、剩余距离和下一步文字提示；
- 仅在在线算路失败时，才把 WGS-84 预设点转换为 GCJ-02 并显示灰色虚线降级路线。

## 园内定位与轻量指引

应用声明了精确/大致位置权限和高德定位服务，但不会在启动时自动定位。只有探索页的“开始指引”按钮会触发系统权限请求；结束指引或页面进入后台时会停止定位。

当前实现属于实验用轻量步行指引，不是完整导航 SDK：它提供真实路线、当前位置、剩余距离、偏离提醒和高德返回的下一步文字，但不包含锁屏后台导航、自动语音播报或复杂路口放大图。公园路径在高德路网中缺失时，算路结果仍会受高德基础数据覆盖程度影响，应以现场道路与标识为准。

## 3D 地图显示

项目使用的依赖本身就是高德 3D 地图 SDK。当前默认相机为约 46° 俯视角，并开启建筑、道路箭头、旋转和俯仰手势；路线自动适配视野后会恢复 3D 角度。八家郊野公园内部建筑较少，所以 3D 感主要来自透视道路、周边建筑与用户手势，具体建筑高度取决于高德数据覆盖。

未开启 `MapsInitializer.setTerrainEnable(true)`：高德地形高程引擎与普通 3D 地图引擎不能同时显示，而实验地点属于城区平缓公园，优先保留建筑与稳定的路线展示更合适。

## 视觉与 IP

- 地图状态、路线输入、结果和探索工作台使用半透明渐变、高光描边与柔和投影形成轻量毛玻璃质感；
- 原生高德 `MapView` 与 Compose 分属不同渲染层，因此没有对地图做高开销的实时背景取样模糊，避免影响旧设备帧率和录屏稳定性；
- 原创银杏叶向导“银小叶”的主视觉位于 `app/src/main/res/drawable-nodpi/ip_ginkgo_guide.png`；生成式 4×5 序列帧位于 `ip_ginkgo_sprite_v2.png`，包含待机、指路、观察、倾听、庆祝五种任务反应。Compose 按帧播放以保留透明度和色彩质量，不使用有 256 色限制的 GIF。
- 每个场景最多只保留一个银小叶；角色只在进入场景或状态变化时播放约 1.4 秒短动作，随后停在自然帧，不会持续循环，也不会在标题、工具卡和地图上重复堆叠。

后续可把实验状态映射到路线颜色、线宽、地标图标、旧路线残影和不确定性范围，但应保证三个实验条件的功能与信息量一致，只改变研究变量规定的呈现方式。
