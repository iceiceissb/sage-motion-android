# SAGE Motion UI 共创与反馈工作流

这套流程用于把“更艺术一点”“还不够好看”转成可验证的界面改动，同时保护实验条件、任务结构和可用性。

## 最省力的反馈方式

不需要写设计文档。每轮只给下面五项，截图上直接画圈、打叉或写数字也可以：

1. **目标屏幕**：这轮只选 1–2 个屏幕。
2. **必须保留**：最多 3 项，例如地图面积、按钮位置、银小叶。
3. **最想改变**：最多 3 项，例如字体、材质、构图、颜色、动效。
4. **参考图**：每张只说明喜欢的局部，不要求整张照搬。
5. **避雷项**：明确不要什么，例如“不要手账感太重”“不要可爱”“不要大圆角”。

可直接复制：

```text
目标屏幕：
必须保留：
最想改变：
参考图 A：我喜欢它的____，不需要照搬____
参考图 B：我喜欢它的____，不需要照搬____
坚决不要：
艺术强度：1 / 2 / 3 / 4 / 5
我希望用户第一眼感到：
```

如果不知道该怎么描述，发 3–5 张喜欢的界面，然后分别说“喜欢这里”，比只给“高级、艺术、好看”等形容词更有效。

## 每轮改造节奏

1. **视觉诊断**：指出当前界面最主要的 2–3 个问题，不先改代码。
2. **方向对齐**：生成不超过 2 个完整屏幕方案；每个方案只表达一个清晰方向。
3. **单屏试做**：先把一个高频屏幕改进 Compose，并输出 390 × 844dp 截图。
4. **对照反馈**：按“保留 / 改弱 / 改强 / 删除”逐项反馈，不要求重新组织专业语言。
5. **系统扩展**：方向确认后再同步到共享主题、状态栏、结果面板和其他任务页。
6. **冻结与验证**：三种实验条件保持相同布局与视觉质量，完成截图回归、单测、Lint 和 APK 构建。

## 视觉反馈的四个旋钮

当结果“差一点但说不清”时，只调下面四个旋钮：

- **艺术强度**：克制工具感 ←→ 展览海报感
- **材质强度**：纯数字界面 ←→ 玻璃、影像、手绘标注或纸张拼贴
- **秩序强度**：规整网格 ←→ 不对称编辑构图
- **角色强度**：银小叶仅提示 ←→ 银小叶承担叙事

每轮最多移动两个旋钮，避免同时更换字体、色彩、构图、插画和动效后无法判断哪项有效。

## 本项目不可突破的边界

- Baseline、Semantic Motion、SAGE Full 的任务结构、信息量、主按钮位置和总计时保持一致。
- 不确定性与备选路线固定使用珊瑚色，不能与主推荐的酸性黄绿色混用。
- 高德 `MapView`、`RouteSearchV2`、定位与原生 `Polyline` 是空间能力底座；视觉重塑只能调整其上层 HUD 和路线样式，不能用静态示意图替代真机地图。
- 正文、触控目标、返回与退出路径不能为视觉表现让步。
- 研究员后台与历史数据页以可靠、可扫读为主，不强制承担艺术主视觉。
