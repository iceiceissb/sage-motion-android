# SAGE App 功能走查与界面微调

> 后续补齐状态请看 [2026-09-19 功能补齐说明](FEATURE_COMPLETION_2026-09-19.md)。本文保留为修改前问题基线。

走查日期：2026-09-18。范围：当前 Android 工程、精简/完整语音版本、FastAPI 后端、功能入口和 Compose 页面。

## 先备份，再调整

调整前全部未提交源码已保存并成功推送到原仓库 `0813` 分支：

[7cb3712 — 调整前快照](https://github.com/iceiceissb/sage-motion-android/commit/7cb3712)

包含此前的语音 flavor 拆分、服务端 skills、多模态上传和部署脚本。未上传被忽略的本机配置、密钥、构建缓存和实验导出文件。本次微调是快照之后的本地变更。

## 判断结论

这是一个具有真实相机、语音输入、地图、数据记录与图片导出实现的研究 Demo；它还不是完整的实时公园智能导游。大部分缺口在于数据源和功能链路尚未接通，不是技术上无法实现。

下面的“已实现”指代码中存在真实执行链路；本次没有连接 Android 手机，且本机没有已配置的模拟器，因此不等于完成相机、语音、定位与现场导航的真机验收。

| 功能 | 当前真实程度 | 当前边界 / 实现所需条件 |
| --- | --- | --- |
| 会话建立、三条件切换、任务重置、并行探索 | 已实现 | 状态机、本地交互、研究员控制台均有实现；实验模式的 AI 阶段和等待时间是可复现脚本。 |
| 逐任务问卷、计时、次数、历史、CSV/ZIP | 已实现 | 本地日志、问卷和 UTF-8 BOM 导出存在；不是云端账号或跨设备同步。 |
| 保存退出、活动会话恢复、历史图片再分享 | 已实现 | SharedPreferences 保存活动状态，日志和已固化的游记 PNG 写入应用私有目录。原始照片仍在 cache 目录，系统清理缓存后可能无法回看原图。 |
| 高德地图、定位、步行路线 | 真正接入 SDK，待真机验证 | 本机配置了高德 Key；需正确包名/签名、隐私同意、权限和网络。截图模式使用静态地图，截图不能证明高德服务已在手机上可用。 |
| 实时导航、当前位置重算 | 部分实现 | 有定位回调、偏航判断和当前位置到固定终点的高德步行查询；不是全目的地导航产品，也没有持续轨迹存储。 |
| 根据遮阴、座椅、安静程度选路 | 内置规则 / 路线画像 | 路线画像和评分是预设的。天气可参与评分，但没有真实树荫时序、座椅库存或实时客流数据。 |
| AI 推荐名称对应地图具体路线 | 尚未闭环 | 高德返回路径列表，界面按第 0/1 条选择；Agent 比较的是另一套命名画像，未按路线 ID、途经点或几何绑定。两者名称相似不能证明指向同一条路线。 |
| 天气、空气质量 | 本次真实联网成功 | 直接调用后端现有 `get_park_environment`，返回 `ok=true`、`is_live_data=true`，耗时约 1.65 秒。查询的是公园固定点，不是手机所在位置。手机侧网络仍需验证。 |
| 附近餐饮地点 | 有真实查询实现，本次未成功 | 直接调用后端 `search_nearby_places`，约 9.23 秒后返回 `ok=false`。不能认定永久不可用，也不能列为已验证可用。Android 与后端有不同的备用查询逻辑；不提供口味评分或可靠营业状态。 |
| 实际拍照 | 已接入系统相机 | `TakePicture` + FileProvider + 应用缓存文件；没有设备，未实拍。 |
| 通用图像标签 | 已接入端侧 ML Kit | 对整张图片输出类别与置信度。不是物体检测框、分割、精细植物鉴定，也不返回标签位置。 |
| 照片上的识别气泡 | 可视化排版 | 气泡使用预设坐标，不是检测到的物体位置。本次移除了“无结果却显示虚构标签和置信度”的默认气泡。 |
| 画圈识别选中物体 | 当前不能实现该效果 | 圆心/半径仅在 Compose 内部保存，不传给推理层，也没有裁剪。后续回答仍按整图最高置信标签选择本地模板；必须补裁剪坐标变换和选区推理链路。 |
| 语音转文字、编辑、确认、TTS | 已接入，设备相关 | slim 使用手机语音服务；full 带 Vosk 中文模型。手动输入可用。能转写不代表回答来自大模型；TTS 还依赖设备语音引擎。 |
| 自由语音问答 | 当前主要为本地关键词路由 | 没配后端时，常见问题命中固定回答，并可拼接天气或地点数据；不能回答任意问题。距离、厕所、花境等固定内容不是当前位置测量结果。 |
| 云端大模型 / 整图理解 | 接入代码存在，当前构建未启用 | 本机 `local.properties` 只有 SDK 路径和高德 Key；生成的 BuildConfig 中后端 URL/客户端令牌为空。工程内也没有 backend/.env。需要部署后端并配置服务端模型凭据及客户端地址。B1 后续“圈搜问答”目前仍走本地模板，部署服务并不会自动补齐它。 |
| 发现封路、自动改道 | 演示 + 部分地图能力 | 封路/降雨变化主要为实验事件或用户描述；没有道路封闭事件源。采纳改道主要切换备选路线，不保证避开用户指出的障碍。 |
| 知识路线 / 照片节点 | 真实记录 + 示意位置 | 真实保存照片和问答，节点按比例排在高德规划路径上；无拍摄时 GPS 或实际行走轨迹。顺序也按照片/语音/改道类别组装，不能作为真实时间空间轨迹。 |
| 本地纸刊和游记 PNG 分享 | 已实现本地绘制 | Compose / Android Canvas 排版。分享 PNG 使用静态底图和示意路线，含固定“约 850m”，并非屏幕高德地图快照，也非同款纸刊导出。 |
| AI 生成式纸刊、自由编辑成品 | 尚未接入 | 有方案文档和端内预览，无图像生成接口或完整编辑器；服务端 C 场景整理文本不等于生成图片。 |

## 本次完成的微调与修正

- 保留深色 HUD、荧光黄绿主操作、青色语音、珊瑚色改道的原有视觉语言。
- 减弱卡片描边、扫描线和等高线，完成态移除重复波形装饰，处理中保留过程提示。
- 探索工作台标题减重，入口文字由 11sp 增至 13sp、次数由 9sp 增至 11sp，增加工具区间距。
- 历史页导出、分享和删除确认按钮改为遵循按钮自身文字颜色，修复亮色主按钮上白字对比度不足的问题。
- 路线卡改为展示实际返回的标题与摘要；移除无指标时补出的“3 处座椅 / 12 分钟”等默认内容，备选路线不复用推荐路线指标。
- 来源文字保留实时/缓存后缀，路线不确定性允许两行，指标区增加高度，改善小字和截断。
- 圈选页明确说明“标签来自整张照片，圈选仅作标记”；回答标注为本地标签解读，知识路线注明节点不代表实际拍摄位置。
- 实拍的“花”标签不再套用固定粉红月季样例描述；本地模板不再凭标签编造颜色、材质和出现原因。
- 未配置有效后端地址时，不再显示云端上传开关和“让 GPT 看图”按钮文案，继续提供端侧识别。
- 远程 SSE 正常关闭但未返回 completed 时，触发本地回退，避免界面无限等待；增加对应回归测试。

## 验证记录

- 后端：22 项 pytest 通过，ruff 通过。
- Android：slim / full 各 24 项 JVM 单元测试通过，包括新增的不完整回答流回退测试。
- Android Lint：0 错误、3 个警告；警告详情见 `app/build/reports/lint-results-slimDebug.txt`。
- `assembleSlimRelease`：成功；产物为 `app/build/outputs/apk/slim/release/app-slim-release.apk`，arm64，沿用现有研究版签名。
- Compose：24 张页面渲染，检查主要页面及 360 × 740dp、1.15 倍字体的路线结果 / 探索工作台，关键按钮未被截断。截图基线位于 `app/src/slimDebug/screenshotTest/reference/`；这是布局检查，不能证明所有设备交互可用。
- 无手机和模拟器：没有验证实际相机拍摄、麦克风识别、定位精度、签名对应的高德鉴权、户外可见性、全程触摸操作、断网后的设备行为。
- 未配置模型后端：没有真实调用付费模型，不能把后端 mock/契约测试解释为线上大模型联调通过。

## 后续实现优先级

1. **先打通路线一致性。** 给高德候选路线建立稳定标识，把推荐、备选、采纳、改道和历史统一绑定到同一个路线对象；地图返回一条路线时不应提供无效的第二条选择。
2. **再接通主 Agent。** 配置后端后验证完整 SSE、文字问答、授权整图理解和失败降级；让 B1 后续提问真正带上图片/选区进入同一个远程会话。
3. **补真实圈选。** 把屏幕圈选范围映射到原图，裁剪并推理；如需气泡指向物体，另补检测框或分割结果。
4. **统一游记来源。** 采集经授权的位置与时间，把原图从缓存转为持久文件；共享地图/路线数据给屏幕和导出，去掉固定里程，之后再接生成式纸刊服务。
5. **现场能力按数据源交付。** 实时树荫、客流、封路和设施状态需要实测、园区数据或相应服务。仅增加大模型不能保证这些现场信息真实可靠。

另需关注：端侧识别无标签/失败时，现有任务 API 用空标签表示固定刺激，仍可能在结果/依据层回退到月季样例；应在下一轮将“没有照片”和“实拍但识别失败”建模为不同输入状态。本次已去掉虚构气泡并收紧本地解读，没有将其宣称为完整修复。
