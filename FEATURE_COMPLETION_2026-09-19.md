# SAGE Motion 功能补齐 · 2026-09-19

修改前代码已推送 GitHub：`0813` 分支的 `7cb3712`。本轮是在该备份之后继续完成的功能修改，保留在本地工作区。

## 本轮落地

| 功能 | 补齐后的行为 | 验证范围 |
|---|---|---|
| 路线选择 | 高德路径具有稳定 ID；结果卡、采纳状态、地图与恢复会话引用同一路线。距离和预计时间来自 SDK。只有一条路径时隐藏备选；没有不同路径时只能保留原路线。 | 编译、纯逻辑测试；实际 SDK 路由待真机 |
| 圈选识别 | 手势圈选转换为原图坐标，按中心裁切显示规则映射；处理照片方向后裁剪圈选外接矩形，再交给 ML Kit。 | 映射、边界、非法输入测试；手势与相机待真机 |
| 照片问答 | 使用圈选结果和同张照片最近问答；授权后可把裁剪图片传给远程模型。没有授权或远程失败时说明仅有本地类别线索，不返回固定花卉答案冒充实拍识别。 | 客户端编译、回退测试、后端图像输入契约测试 |
| 照片持久保存 | 新照片放入应用私有持久目录；仍存在的旧缓存照片在恢复活动会话时迁移。缩略图、圈选、上传、分享使用一致的方向处理。 | 编译与代码检查；卸载应用仍会删除私有数据 |
| 定位与发现记录 | 开启园内指引后，保存有效定位点及照片、语音、已采纳改道事件。过滤过期/低精度定位；超过 90 秒的采样间隔或明显跳点断开绘制。 | 定位过滤、断点测试；真实 GPS 待验收 |
| 知识路线与分享 | 地图只给有定位的事件放置地理标记；其余记录留在列表。导出 PNG 使用真实规划几何和定位记录，分色显示，不再绘制固定 850 米线路。 | 编译和数据绑定检查；PNG 分享待真机 |
| 历史与 ZIP | 会话保存知识游记、AI 纸刊；ZIP 附带原照片和空间数据 JSON。删除历史会话同步清理关联资源。 | 编译与代码检查；完整设备存储流程待验收 |
| AI 拾景纸刊 | 新增照片授权、生成等待、失败反馈、图片预览、分享与历史回看；后端新增 `/v1/journey/zine`，调用图像编辑接口，使用选中照片生成 PNG。 | 认证、禁用、输入校验、上游错误及模拟成功测试；未调用付费图像服务 |
| 附近地点 | Overpass 单次超时后切换另一节点，增加有界 Nominatim 备用查询，结果按半径过滤并缓存；全失败时不编造地点。 | 模拟故障/缓存测试；本轮实测返回 5 个地点，约 3.2 秒 |
| 配置检查 | 新增 `check-backend.ps1`，检查存活、就绪和功能开关，不触发模型调用。部署脚本增加 `-EnableZine` 和图像模型参数。 | 无密钥本地服务实测：health 200、ready 503、agent/zine 均 false |

## 当前仍需外部条件

1. **云端看图、开放式问答、生成式纸刊尚未在这个安装包中启用。** 当前没有项目后端地址和服务端 API 配置；不能把模拟接口测试当成模型实际调用成功。下方已有完整接入步骤。
2. **没有连接 Android 手机或可用模拟器。** 相机方向、圈选手势、系统/Vosk 听写、定位权限、真实导航、分享面板及重启恢复，需要设备验收。
3. **路线按实际距离排序。** 没有树荫、座椅、人流、封路的可靠数据，不能承诺满足这些偏好或自动绕开某个障碍。动态调整只比较 SDK 已有候选路径；没有不同候选时保留当前路线。
4. **仅记录前台园内指引期间的位置。** 切换工具页、关闭指引、后台或失去定位后，不保证连续轨迹；照片/语音使用最多 60 秒内且精度不超过 50 米的最近定位。坐标为高德 GCJ-02，不能直接当 WGS-84 使用。活动状态保留最近 3,000 个定位点、300 个空间事件。
5. **ML Kit 输出通用类别，不是植物鉴定或目标检测框。** 浮动类别标签的位置是界面排版；圈选识别使用裁剪外接矩形。需要可靠品种判断时仍应核实。
6. **本机拼贴预览与 AI 纸刊有独立含义。** 知识路线的分享按钮导出本机路线/照片摘要图；AI 纸刊在生成成功后通过“分享这张纸刊”分享所见 PNG。本机拼贴预览不会被标记为云端生成成品。

## 接通云端

在服务器端设置（实际密钥只写服务器环境或 Secret Manager，不写进 APK）：

```dotenv
OPENAI_API_KEY=服务器端密钥
SAGE_BACKEND_AUTH_TOKEN=后端访问令牌
SAGE_ENV=production
SAGE_ZINE_ENABLED=true
SAGE_IMAGE_MODEL=gpt-image-2.5-sunburst
```

在本地未纳入 Git 的 `local.properties` 设置：

```properties
SAGE_AGENT_BACKEND_URL=https://你的后端地址
SAGE_AGENT_CLIENT_TOKEN=对应的后端访问令牌
```

已有 Google Cloud 项目且完成账户与结算配置时，可使用已准备的部署脚本：

```powershell
.\backend\deploy-cloud-run.ps1 -ProjectId 你的项目ID -EnableZine
.\check-backend.ps1
.\build-app.ps1 -GradleTasks @('assembleSlimRelease')
```

本轮没有执行云部署。部署会使用云资源，图像调用也可能使用 API 额度。`/readyz` 只检查配置是否存在，不证明密钥余额、模型权限或实际生成成功；这些需要在部署后用自愿上传的一张照片验收。服务端图像请求不自动重试，避免重复生成。公开发行前仍需把研究用共享令牌改为用户/设备授权。

图像接口默认生成 1024×1536 PNG。接入依据：[OpenAI 图像生成与编辑文档](https://developers.openai.com/api/docs/guides/image-generation)、[图像输入文档](https://developers.openai.com/api/docs/guides/images-vision)。备用地点查询依据 [Nominatim Search 文档](https://nominatim.org/release-docs/latest/api/Search/)，它返回匹配地点而非区域内全部商户；公共接口适用于当前低频演示，规模化部署应使用专属供应商。

## 验证与交付

- Android：slim/full 单元测试各 34 项；Lint 检查；slimRelease APK 构建。
- 后端：29 项 pytest 测试与 Ruff 检查。
- UI：26 个 Compose 截图预览，包含小屏、大字体、单条真实路线和云端未配置状态。
- 安装包：`app/build/outputs/apk/slim/release/app-slim-release.apk`。
- 新增测试覆盖：圈选坐标、无效范围、路线 ID、单路径/改道绑定、实际指标、陈旧定位、轨迹断点、实拍无标签及后端失败。
- Lint 保留原有类别警告：框架 ExifInterface 与仅 ARM 架构的 ChromeOS 支持；不影响 Android 编译。

以上自动检查不替代真机验收，尤其是相机、GPS、文件分享与云端实调。
